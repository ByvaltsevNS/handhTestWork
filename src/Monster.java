public class Monster extends Creature {
    public Monster(int attack, int defense, int health, int minDamage, int maxDamage) throws IllegalArgumentException {
        super(attack, defense, health, minDamage, maxDamage);
    }
}
